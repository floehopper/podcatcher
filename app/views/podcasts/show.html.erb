<div class="container">
  <div class="row">
    <div class="col">
      <h1><%= @podcast.title %></h1>

      <ul class="list-group">
        <% @podcast.episodes.each do |episode| %>
          <li class="list-group-item">
            <%= episode.title %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
